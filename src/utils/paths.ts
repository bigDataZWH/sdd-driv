import * as path from 'path';

const PROJECT_ROOT = process.cwd();

export const OPENCODE_SKILLS_DIR = path.join(PROJECT_ROOT, '.opencode', 'skills');
export const OPENCODE_COMMANDS_DIR = path.join(PROJECT_ROOT, '.opencode', 'commands');
export const OPENSPEC_DIR = path.join(PROJECT_ROOT, 'openspec');
export const SUPERPOWERS_PLANS_DIR = path.join(PROJECT_ROOT, 'docs', 'superpowers', 'plans');
